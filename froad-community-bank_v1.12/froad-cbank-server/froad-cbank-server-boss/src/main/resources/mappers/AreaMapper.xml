<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.froad.db.mysql.mapper.AreaMapper">
	
	<resultMap type="Area" id="areaResultMap">
		<id property="id" column="id" />
		<result property="clientId" column="client_id" />
		<result property="name" column="name" />
		<result property="vagueLetter" column="vague_letter" />
		<result property="treePath" column="tree_path" />
		<result property="parentId" column="parent_id" />
		<result property="treePathName" column="tree_path_name" />
		<result property="isEnable" column="is_enable" />
		<result property="areaCode" column="area_code" />
	</resultMap>
	
	<!-- 查询  Area -->
	<select id="findAllAreaListOnlyIdAndTreePathName" resultMap="areaResultMap">
		SELECT id, tree_path_name FROM cb_area
	</select>
	
	<select id="findById" parameterType="java.lang.Long" resultMap="areaResultMap">
		SELECT id, name, tree_path_name FROM cb_area WHERE id  = #{id};
	</select>
	
	
</mapper>

