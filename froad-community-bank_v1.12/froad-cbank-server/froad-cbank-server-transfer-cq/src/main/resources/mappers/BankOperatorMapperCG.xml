<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.froad.db.chonggou.mappers.BankOperatorMapperCG">

	<resultMap type="BankOperator" id="bankOperatorResultMap">
		<id property="id" column="id" />
		<result property="createTime" column="create_time" />
		<result property="clientId" column="client_id" />
		<result property="username" column="username" />
		<result property="password" column="password" />
		<result property="orgCode" column="org_code" />
		<result property="mobile" column="mobile" />
		<result property="email" column="email" />
		<result property="name" column="name" />
		<result property="roleId" column="role_id" />
		<result property="department" column="department" />
		<result property="position" column="position" />
		<result property="status" column="status" />
		<result property="isReset" column="is_reset" />
		<result property="remark" column="remark" />
		<result property="isDelete" column="is_delete" />
		<result property="type" column="type" />
	</resultMap>


	<!-- 添加  BankOperator -->
	<insert id="addBankOperator" parameterType="BankOperator" useGeneratedKeys="true" keyProperty="id">
		<![CDATA[
			insert into cb_bank_operator (
				create_time,client_id,username,password,org_code,mobile,email,name,role_id,department,position,status,is_reset,remark,is_delete,type
			) values (
				#{createTime},#{clientId},#{username},#{password},#{orgCode},#{mobile},#{email},#{name},#{roleId},#{department},#{position},#{status},#{isReset},#{remark},#{isDelete},
				#{type}
			) 
		]]>
	</insert>


	<!-- 批量添加  BankOperator -->
	<insert id="addBankOperatorByBatch">
		insert into cb_bank_operator (
			create_time,client_id,username,password,org_code,mobile,email,name,role_id,department,position,status,is_reset,remark,is_delete
		) values 
		<foreach collection="bankOperatorCGList" item="item" index="index" separator=",">
			(#{item.createTime},#{item.clientId},#{item.username},#{item.password},#{item.orgCode},#{item.mobile},#{item.email},#{item.name},#{item.roleId},#{item.department},#{item.position},#{item.status},#{item.isReset},#{item.remark},#{item.isDelete})
		</foreach>
	</insert>
	
	
	<select id="findByUsername" resultMap="bankOperatorResultMap">
		select * from cb_bank_operator where BINARY username=#{username} and client_id=#{clientId}
	</select>
	
	<select id="findAll" resultMap="bankOperatorResultMap">
		select * from cb_bank_operator
	</select>
	
	<delete id="deleteByClientId">
		delete from cb_bank_operator where client_id=#{clientId}
	</delete>
	
</mapper>

