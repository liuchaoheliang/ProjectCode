<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE sqlMap      
    PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"      
    "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap namespace="goodsType">
	<typeAlias alias="goodsType" type="com.froad.CB.po.GoodsType" />
	
	<resultMap class="goodsType" id="goodsTypeResult">
		<result property="id" column="id"/>
		<result property="name" column="name"/>
		<result property="state" column="state"/>
		<result property="createTime" column="create_time"/>
		<result property="updateTime" column="update_time"/>
		<result property="remark" column="remark"/>
	</resultMap>
		
	 <!-- 增加商品类型 -->	
	<insert id="addGoodsType" parameterClass="goodsType">
		insert into goods_type
		(id,name,state,create_time,update_time,remark) 
		values
		(#id#,#name#,#state#,DATE_FORMAT(NOW(),"%Y-%m-%d|%H:%i:%s"),DATE_FORMAT(NOW(),"%Y-%m-%d|%H:%i:%s"),#remark#)
		<selectKey keyProperty="id" resultClass="int">
			select last_insert_id() as id
		</selectKey>
	</insert>
	
	<!-- 根据ID 查找商品类型 -->
	<select id="getGoodsTypeById" parameterClass="java.lang.Integer" resultMap="goodsTypeResult">
		select * from goods_type where id=#id#
	</select>
	
	 <!-- 更新商品类型 -->
	<update id="updateGoodsType" parameterClass="goodsType">
		update goods_type set update_time = DATE_FORMAT(NOW(),"%Y-%m-%d|%H:%i:%s")
		<dynamic prepend="">
			<isNotEmpty prepend="," property="name">
				name = #name#
		      </isNotEmpty>
			<isNotEmpty prepend="," property="state">
				state = #state#
		      </isNotEmpty>
			<isNotEmpty prepend="," property="remark">
				remark = #remark#
		      </isNotEmpty>
		</dynamic>
		where id = #id#
	</update>
	
	 <!-- 查找所有商品类型-->
	<select id="getAllGoodsType" resultMap="goodsTypeResult">
		select * from goods_type
	</select>
    
    <!-- 根据 Brand条件查找商品类型 -->
	<select id="getGoodsTypeList" parameterClass="goodsType" resultMap="goodsTypeResult">
		select * from goods_type
		<dynamic prepend="where">
			<isNotEmpty prepend="and" property="id">
				id = #id#
      	  </isNotEmpty>
			<isNotEmpty prepend="and" property="name">
				name like concat('%',#name#,'%')
	      </isNotEmpty>
			<isNotEmpty prepend="and" property="state">
				state = #state#
	      </isNotEmpty>
			<isNotEmpty prepend="and" property="remark">
				remark like concat('%',#remark#,'%')
	      </isNotEmpty>
		</dynamic>
		order by create_time desc
	</select>
    
</sqlMap>